{
  "name": "pictophone-server",
  "version": "1.0.0",
  "description": "Server to handle Pictophone game logic with socket.io",
  "main": "server.js",
  "scripts": {
    "test": "jest --watch",
    "start": "node server.js",
    "dev": "DEBUG=socket.io:client,socket.io:socket nodemon --inspect server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "jest": "^26.0.1",
    "nodemon": "^2.0.3"
  }
}
